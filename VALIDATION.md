# RoomPilot 0.4.0 驗收紀錄

驗證日期：2026-09-05。Windows 11 x64、Python 3.13.12、PySide6 6.11.2、NumPy 2.3.5、SciPy 1.17.1。

## 自動化與介面

- 完整測試 **197 項通過，56.80 秒**。包含既有解析、品質、保存與驗證，以及新求解器、三策略版本、原子保存、取消、重複計算去重、精確策略識別、群組刪除還原與手動編輯另存。
- 新求解器驗證 24 段實際啟用、逐位置完整 EQ RMS 限制、精修後重開新增、固定目標及保留濾波器、量化後密集限制、已知寬低處增益恢復、深低處／位置不一致保護及取消。不是只測輸入欄位可填大數字。
- 已知 −2 dB／Q1.5 的寬頻低處，在三位置一致且允許增益時恢復 +2 dB／Q1.5。前級依合成曲線計算，單段／總增益與額外餘裕分開；手動編輯也驗證增益尾端及低處 RMS。
- QtTest 以實際 Baseline 隔離副本操作：一次生成一個可見版本與三組完整策略、沒有儲存步驟、標籤切換曲線、重開仍有三組、查看不改已套用識別碼、手改另存且原組不變、L／R 圖切換。**零 QML 警告**。
- 窄介面驗證：1100／1360 像素宽、128 Band 與前級欄位、無前級能力時禁用增益計算、舊模型解釋、shape 指標，以及搜尋路徑／沿用解來源說明。
- 獨立審查的四項 P2 已修正：延伸手動目標衝突、手動增益尾端限制、前級能力提示与行為不一致、搜尋路徑身分未顯示。前兩項新增回歸測試。
- 隨機 64 段、8k／48k／768k Hz 的新向量化響應與既有複數 biquad 計算交叉核對，最大差分別約 2.08e−10／1.16e−8／1.57e−7 dB，均小於 1e−5 dB。

## 使用者 Baseline 唯讀回歸

沿用 v6 條件：左右独立、30–200 Hz、每側最多 10 段、單段最多減益 6 dB、合成最多減益 9 dB、Q 0.4–9、深入搜尋。三組共用自動參考 80–200 Hz，目標 **76.724329 dB**；輸入額外減益總容許量 1 dB RMS。

| 策略 | 策略 RMS 上限 | 殘留波峰 RMS | 低處 RMS：位置最大值 | L／R Band |
|---|---:|---:|---:|---:|
| 保護低處 | 0.35 dB | 1.466122 dB | 0.348566 dB | 5／4 |
| 平衡削峰 | 0.65 dB | 0.923157 dB | 0.649712 dB | 4／4 |
| 充分削峰 | 1.00 dB | 0.612785 dB | 0.999127 dB | 4／6 |

- 三組約 **9.62 秒**。原 v0.3 削峰優先為 0.926491／0.681550 dB；新充分削峰以較多低處影響換得較小残留峰，不把這個取捨說成所有方面都更好。Q 不再全為 9，但仍有邊界或重疊 Band。
- 選平衡削峰，保留參數延伸至 500 Hz：三組約 **7.63 秒**；共同原參數與目標精確保留，利用剩餘段數增加其他頻段修正。原頻段變化最多 L 0.4993／R 0.0138 dB，符合 0.5 dB 限制。整組各位置低處 RMS 均符合各自上限。
- 完整頻段變為 500 Hz 時，低處集合與其 RMS 分母也不同；保留 EQ 在此域已用約 0.739 dB RMS。因此延伸三上限按剩餘容許量計為約 0.830／0.909／1.000 dB，不把 200 Hz 指標直接與 500 Hz 混比。
- 前後讀取使用者 SQLite 均為 mode=ro、query_only；專案 payload SHA256 一致。所有寫入與 GUI 試用只用隔離副本。

## 打包後執行檔驗證

- RoomPilot.exe 版本 **0.4.0** 直接解析實際附件並產生新版 schema2 三策略群組，自檢 **16.498 秒**、exit 0。
- 三組完整參數、獨立策略識別碼、共同群組與預設策略，以及 Baseline、原量測和原始檔 SHA256，均通過專案包匯出／再匯入比對。
- 執行檔使用隔離資料目錄啟動並擷取畫面成功，日誌無 QML 警告。沒有開啟 REW，也沒有使用正式專案作寫入測試。
- 此打包診斷使用預設最多 5 段／Q 上限 6，與上方真實 v6 條件回歸的 10 段／Q 上限 9 不同，不混用其計算數字。

## 解析與外部限制

實際附件內建 MDAT V2 解析：5 筆各 54,533 點，30.029296875–20000.244140625 Hz，逐筆 Mic Cal 已載入。不需啟動 REW、REW API 或 Java runtime。

尚未完成官方 REW 文字逐點交叉驗證、跨 REW 版本廣泛相容性、全新 Windows 電腦驗收，以及套用後／偏移位置的聲學與聆聽驗證。上述為本機、已量資料上的模型預測，時間非效能保證，沒有全域最優主張。私人量測與專案資料不隨發行套件提供。
