# RoomPilot 0.1.0 驗收紀錄

驗證日期：2026-09-05。環境：Windows 11 x64、Python 3.13.12、Qt / PySide6 6.11.2。

## 已通過

- 自動化測試 97 項：內建 MDAT／文字匯入、Mic Cal、無效資料拒絕、品質檢查、獨立／共用 PEQ、步進與總 Gain 限制、深入搜尋取消、手動調整重新評估、補錄比較、專案封存完整性與版本參照。
- QtTest 介面流程：1100×760 與 1360×920，新建專案、清單保存、實際 MDAT 匯入、選取 P0 L/R、確認 Baseline、產生 PEQ、修改為新版本、確認套用、歷史快照。零 QML 警告。
- 打包後 Windows 執行檔啟動與畫面輸出成功；不使用 Python 安裝路徑提供的應用程式模組。
- 打包後的核心測試成功：直接解析附件、計算 PEQ、保存原始檔、匯出再匯入 `.roompilot`。此流程不啟動 REW，也不使用 API 或 Java runtime。
- 原始 MDAT 雜湊與修改時間未改動；發行套件不包含私人量測檔。

## 附件讀取結果

5 筆量測，每筆 54,533 頻率點，30.029296875–20000.244140625 Hz。5 筆均保存 Mic Cal，內嵌校正曲線各 615 點。沒有足夠原始證據確認 clipping／headroom，因此保留未知狀態。

以 5 Band／左右獨立、30–200 Hz、只減益預設計算，實際採用每側 2 段。預測範圍內 RMSE 約 8.44 → 8.22 dB；改善約 0.22 dB，小於 A/B 重錄差異約 3.20 dB，程式會提醒。這些是演算法指標，不能宣稱現場已改善。

## 尚未完成的外部驗收

- 官方 REW 文字匯出逐點數值交叉驗證；目前頻率、SPL 與 Cal 語意是以 REW 5.31.3 靜態程式碼及實際 Cal 顯示核對。
- 真實套用到 DSP 後的補錄與聆聽結果。補錄判定邏輯已用可控合成資料驗證，使用者尚未提供現場校正後量測。
- 廣泛 REW 版本與全新 Windows 電腦的相容性測試。詳見 `KNOWN_LIMITATIONS.md`。
