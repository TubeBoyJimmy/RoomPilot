# RoomPilot 0.5.0 驗收紀錄

驗證日期：2026-09-05。Windows 11 x64、Python 3.13.12、PySide6 6.11.2、NumPy 2.3.5、SciPy 1.17.1。

閱讀順序：先看 `MODEL.md` 的指標定義，再看下方相同條件的比較。若要重現程式測試，在已安裝開發依賴的版本庫執行 `python -m pytest -q`。真實資料回歸另需原資料持有人提供量測／專案；私人資料不包含在版本庫或發行套件。

## 本次完成與證據範圍

- 完整測試 **241 項通過、0 失敗、0 跳過，約 70.05 秒**。JUnit 記錄的 suite 時間為 70.032 秒；時間只代表本機這次執行，不是效能保證。
- 真實 Baseline 完成 **15 組比較案例**：4 組既有方案唯讀診斷，以及 11 組新 v0.5 求解／延伸／Q 上限案例。**11 組新結果全部通過本次設備、逐筆聲壓與完整 EQ 保護驗算**；歷史方案未通過新上限不算回歸失敗，也不表示已證明其聽感較差。
- 正式 SQLite 只讀取；讀取前後專案 payload SHA256 一致。求解不改原量測或正式專案，介面保存測試使用隔離副本。
- 數值來源是最終含頻段外減益保護的回歸結果，不使用尚未加入完整 EQ 保護的早期輸出。
- 本次以同一組 P0 資料求解及驗算，沒有獨立位置或套用後補錄。已確認的是程式內一致性與明示限制，尚未驗證聲學泛化或聆聽偏好。

## 自動化驗證的重點

既有解析、品質報告、專案保存、三策略群組、取消、刪除還原及手動另存持續通過。v0.5 新增的反例與回歸涵蓋：

1. 原本高於目標的頻點被削到低於目標，會計入新增低處；增益改善低處不能抵銷其他頻點代價。
2. 大範圍平均掩蓋局部削減、同位置重錄平均掩蓋單筆超限、固定 80–200 Hz 部分或完全缺少覆蓋，均有分別驗證。
3. 邊界兩段 `200 Hz / −2.7 dB / Q2.62` 與 `200 Hz / −5.9 dB / Q9`，即使測試中的 30–200 Hz 聲壓保護通過，仍會因完整 EQ 在範圍外約 8.6 dB 削減而拒絕。把範圍外上限放寬後，全範圍局部平均保護仍可獨立拒絕過量削減。
4. 完整 EQ 邊界內外探測點參與最大減益檢查，不重複加權局部平均；批次與單條曲線的限制計算一致。手動驗算另核對密集網格，不能繞過求解限制。
5. 波峰寬度只作搜尋起點；近似等效合併必須滿足完整曲線誤差、主指標容差及所有限制。單段深度能力不足時保留原段並記錄原因。
6. 放寬 Q 上限時保留已知可行起點；500 Hz 分階段搜尋若較差，仍保留已通過相同條件的完整既有方案，包含 200 Hz 以上的段，並分開記錄未採用搜尋。
7. 增益支持檢查每筆原始記錄；同位置平均看似有淺低處，但其中一筆沒有低處時不能直接加益。生成結果與手動重驗的策略成本一致。
8. 搜尋及手動驗算共用穩定 biquad 幅頻式，涵蓋高模擬取樣率的限制邊界；舊政策診斷不追溯強制執行新保護，舊參數不自動改寫。

## 真實 Baseline：相同 30–200 Hz 條件

左右獨立、每聲道最多 10 Band、單段最多減益 6 dB、合成最多減益 9 dB、Q 0.4–9、深入搜尋、模擬取樣率 48,000 Hz。目標固定為 **76.724329 dB**，參考頻段 80–200 Hz。原低處總容許量 1 dB RMS，三策略分別使用 0.35／0.65／1.00 dB。

v0.5 共同新增上限：新增低處 RMS **1 dB**、新增低處最大值 **3 dB**、局部平均減益 **4 dB**、固定 80–200 Hz 平均減益 **3 dB**、指定校正頻段外減益 **3 dB**。所有上限在產生前明示，沒有為了通過而自動放寬。

| 方案 | 殘留波峰 RMS | 新增低處最大值 | 完整 EQ 最大局部平均減益 | 指定頻段外最大減益 | L／R Band |
|---|---:|---:|---:|---:|---:|
| ChatGPT 原版 | 0.939 dB | 4.620 dB | 6.201 dB | 6.984 dB | 3／3 |
| v0.4 平衡削峰 | 0.923 dB | 4.784 dB | 6.796 dB | 7.812 dB | 4／4 |
| v0.5 保護低處 | 1.631 dB | 2.505 dB | 3.870 dB | 2.946 dB | 3／4 |
| v0.5 平衡削峰 | 1.244 dB | 2.718 dB | 3.883 dB | 2.990 dB | 3／5 |
| v0.5 充分削峰 | 1.127 dB | 2.988 dB | 3.994 dB | 2.985 dB | 5／5 |

**這個結果是取捨，不是所有數字同時變好。** 新平衡版保留較多殘留波峰，換取較小的新低處、局部及頻段外削減。它符合新的工程限制，還不能據此宣稱比原版更好聽。原版及 v0.4 仍是有效的歷史參數；事後套用新指標不改變其保存政策。

上表「殘留波峰」仍由同聲道同位置平均曲線計分；「新增低處最大值」則取**每筆原始記錄**各自計算後的最差值。「完整 EQ 局部平均」與「指定頻段外」只看濾波器合成曲線，不推定範圍外的聲壓。以上均不含前級。

先前研究報告部分指標先平均重錄或只在指定範圍計算；此處新增最大值與全範圍指標不能直接與那些數字混稱。局部窗口為 1/3 octave，完整 EQ 檢查範圍為 1 Hz–Nyquist；表中最大值是離散網格驗算結果。

三組逐筆原低處 RMS 最大值為 **0.349962／0.649833／0.998293 dB**，各自低於策略上限；新增低處 RMS 最大值為 **0.360195／0.636443／0.919080 dB**。三組生成約 **24.22 秒**，較多計算與較多 Band 不作聽感優勢保證。

## 500 Hz：保留延伸與整體重算分開驗證

以最終 v0.5 的 30–200 Hz 平衡版為保留基礎，延伸至 500 Hz。**最終版本不需放寬任何新增保護上限即可完成**；目標與原有濾波器參數精確保留。

保留 EQ 在 500 Hz 分析範圍的逐筆原低處 RMS 已用 **0.558153 dB**，因此三組策略容許量為 **0.712800／0.845354／1.000000 dB**。這是按剩餘容許量分配，沒有變更共同目標。

| 500 Hz 方案 | 殘留波峰 RMS | L／R Band | 原頻段最大曲線變化 L／R |
|---|---:|---:|---:|
| 保留延伸・保護低處 | 1.145 dB | 6／9 | 0.406／0.218 dB |
| 保留延伸・平衡削峰 | 1.116 dB | 7／9 | 0.478／0.216 dB |
| 保留延伸・充分削峰 | 1.103 dB | 8／9 | 0.500／0.213 dB |
| 整體重算・保護低處 | 1.470 dB | 4／4 | 原段可重算，不適用保留變化上限 |
| 整體重算・平衡削峰 | 1.159 dB | 3／6 | 原段可重算，不適用保留變化上限 |
| 整體重算・充分削峰 | 1.015 dB | 7／6 | 原段可重算，不適用保留變化上限 |

保留延伸耗時約 **22.07 秒**，最大片段變化的未四捨五入值為 **0.499886 dB**，符合 0.5 dB 上限。兩種流程都通過五項共同保護，但原低處策略容許量與可調參數不同，不能把同名「平衡」當成完全相同實驗。30–200 Hz 與 30–500 Hz 的主指標網格、分母也不同，不以表間 RMS 差直接宣稱改善。

在固定目標、每聲道 3 Band、原低處容許量 0.65 dB 的另一項實驗，Q 上限 9 的殘留波峰 RMS 為 **1.264388 dB**；提高到 12 並提供前者作可行起點後為 **1.173670 dB**，兩者均符合完整保護。這證明這次較寬搜尋沒有丟掉已知可行解，不是高 Q 較好聽的證明。

## 實際 QML 與保存流程

介面驗證使用正式 Main／Bridge 與隔離專案副本，完成以下檢查：

- 透過實際背景工作及完成流程，將已驗算的三策略數值結果回放到介面，原子保存為一個新版本：可見版本由 7 增為 8，新組有三個策略。
- 三個標籤、完整參數、PEQ 與聲壓圖 L／R 切換、新指標、完整 EQ 邊界保護及峰區／參考方案說明可查看。
- 重開後精確還原；原有 9 筆方案的濾波器、設定與解釋保留。正式資料庫 payload 雜湊不變。
- 1360×1020 與 1100×920 介面完成檢查；內容可垂直捲動，沒有水平截斷或元件重疊，**零 QML 警告**。邊界外探測頻率以「上界／下界外」呈現，避免四捨五入後被誤讀成範圍內。

這項介面驗證是 offscreen QML 訊號操作，**回放已檢查的 pool，不是在這輪 UI 再跑一次數值求解**，也不是原生滑鼠端到端自動化。pool 雜湊與最終數值回歸一致；數值求解和保存／顯示證據分開記錄。

## 打包後執行檔

- Windows `RoomPilot.exe` **0.5.0** 自檢通過，約 **20.625 秒**、exit 0：直接解析原始 MDAT，執行 v5 五項保護的三策略計算，逐組驗算並完成專案包匯出／再匯入。
- 三組識別碼、完整參數、群組與策略關聯、Baseline／原量測及來源檔案均通過往返比對；原始來源未修改。
- 此打包自檢使用每聲道最多 5 Band、Q 上限 6；與上方真實比較的 10 Band／Q9 不同，不混用計算結果。
- 執行檔使用隔離資料目錄、offscreen 圖形後端啟動及截圖成功，exit 0，日誌僅有 v0.5.0 啟動紀錄，沒有 QML 警告。套件 QML 與最終原始碼逐檔核對一致。
- 以上是這台 Windows 電腦的可攜版驗證；尚未完成全新 Windows 電腦或不同環境的相容性驗收。

## 解析與外部限制

已知附件為內建 MDAT V2 解析的 5 筆曲線，各 54,533 點，頻率約 30.029296875–20000.244140625 Hz；逐筆 Mic Cal 有保存紀錄。不需要啟動 REW、REW API 或 Java runtime，原保存 SPL 不重複套用 Cal。

尚未完成官方 REW 文字逐點交叉驗證、跨 REW 版本廣泛相容性、全新 Windows 電腦驗收，以及套用後／偏移位置的聲學與聆聽驗證。更多計算無法補回未量資料，模型不能由幅頻分數證明衰減時間、相位或偏好改善；詳細界線見 `KNOWN_LIMITATIONS.md`。
