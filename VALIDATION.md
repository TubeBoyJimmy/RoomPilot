# RoomPilot 0.3.0 驗收紀錄

驗證日期：2026-09-05。Windows 11 x64、Python 3.13.12、PySide6 6.11.2、NumPy 2.3.5、SciPy 1.17.1。

## 自動化與介面

- 完整測試 **153 項通過，19.80 秒**。涵蓋既有 MDAT／文字匯入、Cal、封存、品質提醒、PEQ 配置與限制，以及新版候選比較、逐位置診斷、逐 Band 成本、唯讀舊版解釋、取消、版本去重與保存安全。搜尋紀錄欄位更名後另重跑對應成本重建測試，1 項通過。
- 新版引擎測試使用合成資料，確認固定比較遮罩、不同政策同目標、完整 EQ／保留 Band 納入門檻、disabled Band 排除、門檻不是逐頻點上限、Pareto 僅比較可行候選、手動編輯不繼承舊排名。
- 控制器測試確認生成／預覽不寫入版本；保存才增加一版，再存去重；切換專案、Baseline 或來源內容變動不能誤存舊候選；保存時以目前已套用版本記錄替代關係；內部暫時鎖定目標不會改成使用者的手動目標設定。
- QtTest 實際點擊，以原始 Baseline 的隔離副本執行：三候選預覽、保存、去重、重開歷史、未保存動作禁用、逐 Band 成本與新增輪次、L／R 圖切換、0 門檻保留 0 且不可保存超限方案、手動編輯後清除原比較。**零 QML 警告**。
- 已檢查 1100 與 1360 像素寬畫面，包含比較卡片、指標、濾波器表與說明對話框。
- 獨立程式審查提出的三項 P2 已修正：自動目標來源、保存時的替代版本、未達門檻候選的比較文案。

## Windows 打包驗證

- PyInstaller 可攜目錄建置成功。打包後的 RoomPilot.exe 直接解析原始附件、產生 PEQ 與三個候選，確認共同目標與逐 Band 解釋存在，專案包匯出／再匯入成功；診斷用時約 21.26 秒。
- 打包診斷使用預設每側 5 Band／Q 上限 6／30–200 Hz，與下方 v6 回歸的 Q 上限 9／10 Band 不同，不混用其結果。
- 執行檔在隔離資料目錄成功啟動並擷取 QML 畫面；日誌版本 0.3.0，沒有 QML 警告。驗證未開啟 REW、未使用使用者正式專案目錄。這不是全新 Windows 電腦驗收。

## 使用者 Baseline 的唯讀回歸

條件與 PEQ v6 相同：左右獨立、30–200 Hz、每側最多 10 Band、單段最多減益 6 dB、合成最多減益 9 dB、Q 0.4–9、深入搜尋。三候選共用自動參考 80–200 Hz，目標 **76.724329 dB**，額外減益容許量 1 dB RMS。

| 候選 | 殘留波峰 RMS | 原低處額外減益（位置 RMS 最大值） |
|---|---:|---:|
| 保護低處 | 1.379634 dB | 0.443426 dB |
| 較多削峰 | 1.085807 dB | 0.600797 dB |
| 削峰優先 | 0.926491 dB | 0.681550 dB |

- 本機單輪生成約 **10.55 秒**；第一候選的目標與六段 PEQ **精確重現 v6**。三候選保留自動目標模式，沒有把內部固定比較目標誤存為手動設定。
- 本組候選的 Q 仍全部為 9。新版讓取捨可比較、可追查，沒有聲稱已消除 Q 觸頂，或已得到經聆聽／套用驗證的最佳結果。
- 選第二候選，以「保留原案，擴充高頻」延伸到 500 Hz：去重後兩候選，約 **3.58 秒**；其中一案增加 L 238 Hz／−1.8 dB／Q9 與 R 221 Hz／−1.3 dB／Q9。原目標與六段參數完全不變，新增裙帶令原範圍合成曲線最多變動 L **0.166 dB**／R **0.307 dB**，在設定的 0.5 dB 內。另一案未增加 Band。
- 200 與 500 Hz 的指標計算範圍不同，不能直接拿其 RMS 當同域改善量；也不能把「沒有新增 Band」解讀成所有可能組合都沒有價值。
- 讀取採唯讀 SQLite；回歸前後使用者專案 payload SHA256 一致，未改寫正式專案。上述時間為本機觀察，非效能保證。

## 解析與外部限制

沿用已驗證的內建 MDAT V2 解析器：附件 5 筆各 54,533 點，30.029296875–20000.244140625 Hz；逐筆 Mic Cal 各 615 點。不需啟動 REW、REW API 或 Java runtime。來源缺少足夠 clipping／headroom 證據時仍保持未知。

私人量測與專案資料不隨程式發行套件提供。尚未完成官方 REW 文字匯出的逐點交叉驗證、跨 REW 版本廣泛相容性、全新 Windows 電腦驗收，以及真實 DSP 套用後與 ±10 cm 位置的聲學／聆聽驗證。候選為有限搜尋、產品政策下的預測，不代表聲學最優。
