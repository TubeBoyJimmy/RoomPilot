# v0.5 的 PEQ 模型與取捨

本文件描述目前程式的實際規則。v0.5 補上逐筆量測與完整 EQ 範圍保護、波峰寬度搜尋起點、濾波器合併驗算與既有可行方案保留；主指標仍是幅頻誤差，沒有宣稱已建立聽感排名或通用最佳 Q。

## 一個版本內的三種策略

「產生 PEQ」會一起計算三組完整方案，全部通過驗算後保存為一個版本，透過標籤切換。三組共用 Baseline、目標、校正範圍、Band、Hz／Gain／Q／步進及新增保護上限；只有「原低處額外減益 RMS」容許量不同。

若輸入總容許量 1 dB RMS，保護低處／平衡削峰／充分削峰分別使用 0.35／0.65／1.00 dB。比例是產品預設；較寬容許量可能得到相同參數，不代表必定更好聽。增益精修沿用三組容許量，另有下述增益證據限制。

延伸已選方案時，先在本次完整頻段求出保留 EQ 已造成的逐筆原低處 RMS 最大值 F。使用者上限為 L，三組改用 `F + (L−F)×35%、65%、100%`。F>L 或其他共同保護已超限時，說明實際數值並拒絕保留延伸；不會自動放寬限制、降低目標或修改舊參數。

## 目標與計分資料

自動目標固定參考 P0 的 80–200 Hz：同聲道重錄以 dB 平均，再取左右中位數曲線的第 35 百分位。這是工程啟發式；擴大校正頻段不會改用另一個目標估算範圍。可手動指定水平，保留延伸則沿用既有目標。

主目標使用同聲道、同位置重錄的 dB 平均曲線。聲道等權；各聲道同時有 P0 與偏移位置時，P0 權重 67%、偏移合計 33%。評估網格以 log2 固定錨點每 octave 192 點，加上實際邊界；診斷使用未另外平滑的 SPL。

設 y 是計分曲線、E 是全部啟用濾波器的合成 dB 響應、T 是共同目標，削峰模式最小化：

`加權平均(max(y + E − T, 0)²)`

殘留波峰 RMS 是其平方根。前級衰減不進入目標；原低處不能以自身深度抵銷波峰。這個分數衡量已量資料上的預測，不能直接代表音量、清晰度或音質。

## 逐筆保護：不讓重錄平均掩蓋超限

v0.5 的保護使用每筆原始記錄各自插值後的曲線，不以重錄平均取代。以下 y 代表一筆原始記錄；每筆都必須通過，畫面取各筆最差值。原本的同位置平均診斷另外保留，不與逐筆值混稱。

| 指標 | 計算與範圍 | 起始上限 |
|---|---|---:|
| 原低處額外減益 RMS | `sqrt(mean(max(−E,0)² | y<T))`；遮罩固定於 Baseline | 三策略各自的容許量 |
| 所有頻點新增低處 RMS | 下式 D 在全部分析頻點的 RMS | 1.0 dB |
| 單一頻點新增低處最大值 | 下式 D 在分析網格的最大值 | 3.0 dB |
| 局部平均減益 | 滑動 1/3 octave 窗內 `mean(max(−E,0))` 的最大值；同時檢查量測範圍與完整 EQ 曲線 | 4.0 dB |
| 固定低頻平均減益 | 80–200 Hz 可用交集內的 `mean(max(−E,0))` | 3.0 dB |
| 指定頻段外合成減益 | 完整 EQ 在要求的 f_min–f_max 以外之 `max(max(−E,0))` | 3.0 dB |

`D = max(0, max(T − y − E, 0) − max(T − y, 0))`

D 包含「原本高於目標，校正後被切到低於目標」的新增低處。最外層 max 保證增益若改善既有低處，不會被算成負的傷害來抵銷其他頻點。局部與固定低頻指標只平均正的減益量，增益不能抵銷別處削減。

新增五項 dB 上限可在進階欄位調為 0–30 dB，屬明示的工程起始值，沒有聲學標準或可聽差異保證。原低處總容許量仍是 0–6 dB。介面局部窗口固定為 1/3 octave；計算設定可接受 1/12–1 octave。空的原低處集合記 0 並記錄數量，不代表沒有其他風險。

新增聲壓低處及原低處保護涵蓋本次實際分析頻段，包含範圍內的所有保留 Band 影響，不含前級。完整 EQ 範圍保護另行覆蓋指定頻段以外，不需要假設外部聲壓。局部窗口包含兩側邊界對齊的完整窗口；若整段不足窗口寬度，就只檢查實際區間並標示不完整。80–200 Hz 沒有完整覆蓋時，明示可用交集；完全無交集則不提供數值或宣稱通過，不外推未量資料。

**設為 30–200 Hz，不代表濾波器在 200 Hz 以上沒有作用。** 因此程式直接限制指定頻段外的完整 EQ 減益，並把局部窗口檢查延伸到整條 EQ 曲線，不能把大量削減移到 200 Hz 之外來規避原頻段保護。這是濾波器影響限制，不是推定範圍外量測已通過。

完整 EQ 網格為 1 Hz–Nyquist，每 octave 192 點並加入兩側校正邊界及其內外探測點；邊界探測點參與最大值檢查，不重複加權局部平均。最終驗算另外在 16,384 點密集網格、濾波器中心與邊界探測點檢查指定頻段外減益及總 Gain。這是離散數值驗算，不是對所有連續頻率的解析證明。延伸到 500 Hz 仍會新增聲壓資料的保護檢查，既有方案可能因資料或限制差異不符合保留條件，程式不會自行放寬。

## 波峰寬度如何影響 Q 與 Band

搜尋仍保留各位置殘留峰、綜合殘留峰，以及每 octave 24 個規則起點。v0.5 另外找出綜合正超額曲線的峰中心與半突出高度兩側位置，記錄寬度、峰高、原始記錄在中心仍高於目標的比例及其高低差。最多取突出度較高的 48 個區域，作為補充起點。

`中心頻率／兩側寬度` 只產生一個受使用者 Q 範圍限制的起始值。搜尋同時試窄／寬及深／淺組合，再聯合調整頻率、Gain、Q；記錄不一致時增加較淺的共同支持起點，不會自動把濾波器加寬。中心支持比例不等於整個峰在其他座位都穩定，也不是衰減時間或模態 Q。

沒有「Q 越低越好」或「Q 越高越好」的罰分，也不自動降低使用者 Q 上限。沒有每段固定成本。每輪新增後會精修、再重新檢查新增；深入模式每輪最多精修 6 個初選，12 段內聯合精修，更多段用座標精修；另一條同時配置路徑最多以 24 段起始，後續仍可增加至使用者 Band 上限，產品運算上限為 128 段。

新增起始門檻是主指標 RMS 改善 0.005 dB。停止只表示有限搜尋未找到達門檻且符合限制的新增；不是剩餘波峰已無法處理或全域最優。主目標近乎相同時，才以較小 EQ 幅度作次要選擇。

## 重疊 Band 與近似等效合併

相同中心的兩個 PEQ 並不等於把 Gain 相加、Q 不變的一個 PEQ。v0.5 會對接近、同符號且 Q 相近的未鎖定段嘗試重新擬合一段，取整後比較完整合成曲線。

合併需同時符合：密集網格最大曲線差 ≤0.05 dB、曲線差 RMS ≤0.01 dB、主指標 RMS 惡化 ≤0.002 dB，以及所有設備與逐筆保護限制。這是可檢驗的近似等效容差，不是完全相同的傳遞函數或可聽閾值。

若合併所需深度已超過單段 Gain 能力及曲線容差，直接記錄原因而保留原段。每輪合併檢查最多 64 組；沒有保證消除全部重複段。保留延伸的鎖定段不會被取整、合併或重排。每次嘗試保存替代參數、誤差及接受／拒絕原因。

## 既有方案、頻段配置與可比較性

既有方案只有在目標、左右設定、模擬取樣率相容，且原參數完整通過本次 Band／步進／Gain／Q／逐筆保護驗算後，才作為可精修的搜尋起點。參數不符合目前步進時不會默默換成取整後的另一個起點。

較寬容許量包含前一策略的可行解，因此程式會保留該起點，並在結果較差時沿用已知可行完整參數。另對低頻優先的多階段流程比較最終完整方案，避免既有 200 Hz 以上 Band 未能進入第一階段而讓整組退步。此保留機制只保障相同指標與條件下的已知可行比較，不能保證新增搜尋找到最佳解。搜尋嘗試和實際沿用的來源分開記錄，成本重新驗算。

三種頻段配置的區別仍是：低頻優先先配置 ≤200 Hz，再以剩餘 Band 延伸；保留延伸固定已選完整參數；整體重算允許整個指定頻段共同調整。新增段對保留頻段的曲線變化受使用者上限限制，預設為 0.5 dB。Band 不足時保留並提示，不擅自捨棄原段。

500 Hz 保留延伸若因新增檢查區間發現舊曲線超限，會列出指標、實際頻段、數值與上限；可由使用者明確調整限制或選擇整體重算。放寬限制和重新分配原 Band 是不同實驗，程式不會自動混用。

有參考方案時，另在量測可用的固定 80–200 Hz 比較完整 EQ 的平均 Gain 差、1/3 octave 局部平均差，以及各自平均減益；不含前級，不改變目標或保護值，不把較接近參考判為較好聽。校正後聲壓曲線也提供 1/3 octave Gaussian 寬頻走勢診斷，目前不列入最佳化成本。

## 增益精修、數值與尚未實作的範圍

啟用增益後，主目標加入具支持低處的誤差。需要 P0、P−10、P+10 基準；v0.5 在每筆原始記錄上檢查寬頻淺低處支持，不能用平均掩蓋其中一筆沒有低處。沿用 Q≤2、避開校正下限最底半個 octave、深低處不補及支持區外尾端限制。缺少所需位置時，新加濾波器只減益。這仍不是 minimum-phase 辨識。

前級由整組最大合成增益加上設定餘裕估算，向下取到 0.1 dB 衰減；不進入目標或上述保護。不保證所有節目訊號或 intersample peaks 不削波，也不增加喇叭輸出能力。

PEQ 採 RBJ peaking biquad 的 float64 幅頻模型，搜尋與手動驗算共用穩定的數值計算式；受限制精修使用 SciPy SLSQP。DSP 取樣率欄位是濾波器模擬條件，不是設備最高支援規格，也不會控制播放器重取樣。[RBJ Audio EQ Cookbook](https://www.w3.org/TR/audio-eq-cookbook/)

舊版保存參數及政策不會自動改寫；沒有 `guard_policy="v5"` 的歷史資料仍按原政策理解，新診斷會標示未在當時強制執行。逐 Band 移除比較不重算其餘參數，不宜單靠一項成本差刪除互相補償的段。

本版沒有建立完整 house curve、喇叭自然衰減模型、相位／衰減時間目標、房間脈衝逆濾波或 GPU 路徑，也沒有把主觀偏好學習成評分。更多運算、更多 Band 或較低的誤差都不能補回未量測的空間資訊；同條件補錄及可比聆聽仍用來確認預測的實際效果。
