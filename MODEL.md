# v0.4 的 PEQ 模型與取捨

本文件描述程式實際採用的規則。它們是可驗證的工程政策，沒有宣稱為跨空間的聲學最佳解。舊版的成本與參數不會因更新自動改寫。

## 一個版本內的三種策略

按「產生 PEQ」後，三種策略一起計算並保存為一個版本；點選標籤切換完整參數。它們共用 Baseline、目標、校正頻段、可用 Band 與設備限制，只有低處額外減益的容許量不同。

例如輸入總容許量 1 dB RMS：保護低處使用 0.35、平衡削峰使用 0.65、充分削峰使用 1.00。這三個比例是產品預設。提高容許量並不強迫產生不同參數，也不代表一定更好聽。

延伸已選方案時，保留完整舊 EQ 可能已用掉部分容許量。設它在本次完整頻段的低處 RMS 最大值為 F、使用者上限為 L，三組上限為 F + (L−F)×35%、65%、100%。若 F>L，先要求調整範圍或限制，不能默默降低目標或改寫舊濾波器。

## 主目標與限制

同聲道、同位置的 A/B 重錄先以 dB 平均；在每 octave 192 點的固定對數頻率網格上評估。聲道等權；每聲道同時有 P0 與偏移量測時，P0 為 67%、偏移合計 33%。

設 y 為原量測、E 為所有啟用濾波器的合成 dB 響應、T 為共同目標。削峰模式最小化：

`加權平均(max(y + E − T, 0)²)`

畫面的殘留波峰 RMS 是其平方根。原本已低於目標的頻點不會因自身的深度而抵銷凸峰；前級衰減也不參與計分。單純降低整體音量不會成為模型的改善手段。

另對每個已量位置，固定取 Baseline 原本 y<T 的頻點，檢查：

`sqrt(mean(max(−E, 0)² | y<T)) ≤ 該策略容許量`

這項條件在求解、取整、最終驗算都要符合。包含全部保留 Band，不含前級；空集合記 0 並保留數量。它是這些頻點的 RMS，不是每個頻點最大只能削同樣的 dB。

其他限制包括每段 Hz／Gain／Q／步進、Band 數、整組合成 Gain，以及延伸時對原頻段造成的變化上限。主目標不再加上每段 0.025 的固定成本。

## 如何使用更多 Band

搜尋先掃描各位置的殘留峰、綜合殘留峰及規則的對數頻率起點，批次比較不同 Q 與 Gain，再對可行候選做連續精修。每次新增後可共同調整現有未鎖定段，精修後重新檢查能否新增。深入模式另比較一條同時配置多段的路徑，最終採用較佳可行結果。

這比舊版「停止新增後只精修一次」涵蓋更多可能性，但仍是有限搜尋。當前上限：每 octave 24 個規則起點、深入模式每輪 6 個初選精修、12 段內聯合精修，更多段使用座標精修；同時多段路徑最多以 24 段起始，後續仍可增到使用者指定的 128 段上限。

新增主指標 RMS 改善門檻為 0.005 dB。近乎相同的主目標才以較小 EQ 幅度作次要選擇；移除某段若不惡化主目標且仍符合限制，也可減少冗餘。這些規則都不等於可聽差異標準。

重疊或相同中心的多段濾波器可以構成單段不易達成的曲線；不能只把 Gain 相加就假定能完全等效合併。應看完整合成響應，而不是把每段都解讀為獨立房間模態。

## 前級衰減與增益精修

允許增益時，主目標增加「具多位置寬頻支持之低處」的平方誤差。正增益中心需由 P0、P−10、P+10 基準支持，Q≤2，避開校正下限的最底半個 octave；深低處或不一致區域不列為增益需求。支持區外的合成增益尾端也受限制。缺少偏移基準時會說明原因，新加濾波器只減益。

單段增益上限與多段合成增益上限分開。前級建議以整組曲線在完整 Nyquist 內的密集網格及濾波器中心求最大合成增益；大於 0.01 dB 時，加上使用者設定的額外餘裕，向下取到 0.1 dB 衰減。這是幅頻峰值估算，不能保證所有節目訊號或 intersample peaks 不削波。

前級預留數位餘裕，不會使實體喇叭有更多低頻輸出能力，也不能修復聲學抵消。REW 同樣分開處理 individual／overall boost 與濾波器 headroom；Roon 提供前級餘裕與削波指示，實際播放仍需核對。[REW EQ 設定](https://www.roomeqwizard.com/help/help_en-GB/html/eqwindow.html)、[REW 濾波器 headroom](https://www.roomeqwizard.com/help/help_en-GB/html/eqfilters.html)、[Roon Headroom Management](https://help.roonlabs.com/portal/en/kb/articles/dsp-engine-headroom-management)。

## 可追查與可驗證的範圍

每輪紀錄原值、候選值、限制與原因；順序路徑和同時多段路徑分開標示，路徑內接受不代表最終採用。若較宽容許量的搜尋沒有改善較嚴策略的已知解，保留該可行參數並明示來源。

逐 Band 比較只移除該段，不重算其他參數；增減益可能互相補償，不能只看某個成本差就刪除。手動修改使用相同限制重新驗算，另存一個獨立新版本。

PEQ 幅頻使用 RBJ peaking biquad 模型；數值以 float64 計算，受限制連續精修採 SciPy SLSQP。上述求解方法能處理不等式約束，不提供非凸問題的全域最優保證。[RBJ Audio EQ Cookbook](https://www.w3.org/TR/audio-eq-cookbook/)、[SciPy SLSQP](https://docs.scipy.org/doc/scipy/reference/optimize.minimize-slsqp.html)。

可用 Band、運算時間或 GPU 都無法補回未量測的空間資訊。改善數字是對已量資料的預測；相同條件補錄與其他位置量測仍是確認結果的必要步驟。
