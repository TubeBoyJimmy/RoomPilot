# RoomPilot 0.5 · 已知限制

本文件區分目前已實作的行為與尚未驗證的範圍，避免把可讀檔案、預測改善或使用者確認當成完整量測保證。

## 檔案解析與相容性

- 正常 `.mdat V2` 匯入使用內建被動 Java 資料解析器，不需要 REW、Java 執行環境或 API，也不會啟動外部 REW 程式。
- 目前支援可辨識的 `REW Measurement Data File V2`／`MeasData` 結構與已保存的 SPL、Phase 陣列，保留保存時的平滑／視窗資訊。沒有聲壓頻響的阻抗資料不能用來產生空間 PEQ。
- 未知版本、不同群組／容器結構、不完整陣列、頻率軸不符或損壞資料會被拒絕，並提示可選的 REW 文字匯出途徑。這不表示所有歷史與未來 REW 版本都已支援。
- 單檔上限為 256 MB；單曲線最多 4,194,304 點。可在 REW 分批保存量測，或匯出適用頻段後再匯入。
- 文字匯入支援單筆 Frequency／SPL 與選填 Phase，包含常見空白、tab、CSV、分號及編碼；不支援任意多曲線表格。明確的 dBFS、電壓或阻抗單位應重新匯出為 dB SPL。
- 文字檔缺少的 Cal、取樣率、播放路徑與錄製狀態會保持未知，不會由檔名或曲線形狀補猜成已確認。

## 本次驗證範圍

- 使用者提供的 Baseline 已讀出 `L L_P0_A`、`L L_P0_B`、`R R_P0_A`、`R R_P0_B`、`L+R LR_P0` 五筆曲線；每筆 54,533 點，頻率為 30.029296875–20000.244140625 Hz。
- 各筆獨立 Mic/Meter Cal 紀錄均為 `8112838.txt`，內嵌曲線各有 615 點；此狀態已在 REW 官方 GUI 確認。原始檔沒有改寫。
- 已靜態核對 REW 5.31.3 的頻率軸、SPL 陣列與 Mic Cal 判斷方法。保存的 SPL 不會再重複套用一次 Cal 或 SPL offset。
- **尚未完成官方 REW 文字匯出的逐點數值交叉驗證。** 目前測試不能替代廣泛的 REW 版本、麥克風、音訊路徑與不同類型量測的相容性驗收。
- 私人量測資料不包含在發行套件；以這份檔案為基礎的測試需由持有人提供資料才能執行。

## 品質報告的界線

- 「Cal 已載入」代表量測保存的校正資訊，不能驗證實體麥克風序號、朝向或當時是否使用了正確校正檔。REW 也允許錄完後補上或更換 Cal。
- Mic/Meter Cal 與 Soundcard Cal 分開解讀；未使用音效卡 Cal 不能直接當成漏載麥克風 Cal。
- 本次 MDAT 沒有足以確認錄製 clipping／headroom 的原始紀錄，這些項目會標示未知；不使用正規化 IR 峰值宣稱無 clipping。
- 從來源取得的 SNR 或失真摘要是 REW 保存的分析資訊，不等於完整背景噪音錄音或各頻點的失真保證。
- 裝置名稱由作業系統列出；它無法自動辨識 DSP 的 Band 數、Q 定義、通道能力、內部取樣率，或掃頻是否實際通過 DSP。
- 環境、位置、主音量與播放路徑中的使用者紀錄是提醒與比對依據，不是軟體實際監測結果。按下確認提醒不會把未知項目改成通過。

## PEQ 與補錄

- v0.5 仍以殘留波峰平方平均為主要目標；三策略只改變原低處額外減益 RMS 容許量，預設為使用者總上限的 35／65／100%。沒有已驗證的音質排名，不能由數字判定較自然或較有低頻重量。
- 主目標仍將同位置重錄以 dB 平均，聲道等權，有 P0 與偏移時採 67／33 權重。v0.5 的保護及增益支持則對每筆原始記錄各自驗算，原本的平均曲線診斷另外保留；「最差 RMS」是各筆記錄 RMS 的最大值，不是最差頻率或聲道音量差。
- 原低處 RMS 的固定遮罩仍有盲點，因此新增所有頻點的新增低處 RMS／最大值、局部窗口、固定低頻平均減益及指定頻段外完整 EQ 減益限制。新增低處為 `max(0, max(T−y−EQ,0)−max(T−y,0))`；外層 max 不允許增益改善抵銷其他頻點傷害。這些指標可作限制，尚不能替代聽感評分。
- 新增低處 RMS／最大值、局部平均、固定低頻平均及指定頻段外減益的起始上限依序為 1／3／4／3／3 dB，可明確調整為 0–30 dB；局部窗口介面固定 1/3 octave，固定低頻為 80–200 Hz。這些是工程起始值，並非聲學標準、可聽閾值或設備能力。三策略共享這五項上限，不會因「充分削峰」自動放寬。
- 聲壓新增低處保護只涵蓋實際分析範圍，但 v0.5 另在完整 EQ 曲線限制指定頻段外減益，並把 1/3 octave 局部平均保護涵蓋至 1 Hz–Nyquist，避免用邊界濾波器把削減移出檢查範圍。這不需要推定範圍外聲壓，也不代表未量聲學效果已確認。擴大到 500 Hz 仍會新增聲壓資料的檢查，可能需要重新分配參數。
- 完整 EQ 保護採每 octave 192 點、邊界內外探測點，最大指定頻段外減益另用 16,384 點密集網格及濾波器中心驗算；不是連續全頻率的解析保證。所有曲線保護不含前級，不能以整體前級衰減掩蓋參數代價。
- 固定 80–200 Hz 未完整覆蓋時只檢查可用交集，完全沒有交集則不提供該數值；不外推未量資料或把缺少資料當作 0。局部窗口不足完整寬度時也會標示。所有這些指標不含前級；完整密集頻率網格的總 Gain 檢查另行執行，不能替代缺少的聲壓資料。
- 波峰中心、半突出高度兩側寬度及逐筆中心支持比例只提供頻率／Gain／Q 的聯合搜尋起點，沒有辨識物理模態、衰減時間或未量座位穩定性。記錄不一致不會自動導致較寬濾波器；沒有一律降低 Q 上限，也沒有 Q 高低音質罰分。
- 搜尋包含殘留峰、每 octave 24 個規則起點及最多 48 個補充峰區。深入模式每輪最多精修 6 個初選，12 段內聯合精修，更多段用座標精修；另一路徑最多以 24 段同時起始。Band 上限 128，沒有窮舉所有組合或全域最優保證。0.005 dB 主指標 RMS 新增門檻是停止規則，不是可聽差異。
- 近似等效合併只嘗試接近且同符號的未鎖定段；最多 64 組。需通過最大曲線差 0.05 dB、曲線差 RMS 0.01 dB、主指標 RMS 惡化 0.002 dB 容差及所有限制。單段 Gain 能力不足時保留原段，不能直接以 Gain 相加及原 Q 取代重疊濾波器；也不保證消除所有冗餘。
- 既有方案必須在同目標、通道方式、模擬取樣率及目前全部限制／步進下通過，才用作可精修起點。有限搜尋較差時可保留已知完整可行參數，並分開保存嘗試紀錄；這只防止相同計分條件下退步，不證明更好聽。參考 EQ 的固定 80–200 Hz 比較及校正後寬頻走勢只作診斷，不改變目標或成本。
- 保留延伸固定原參數，不合併、不重排，並限制新增段對原頻段的變化，預設 0.5 dB。剩餘 Band 不足或舊曲線在擴大範圍已超限時會提示實際原因，不默默放寬限制。選擇整體重算才允許原段重新分配；兩者是不同實驗。
- 增益精修需 P0、P−10、P+10 的逐筆寬頻淺低處支持，沿用 Q≤2、避開最底半個 octave、深低處不補及支持區外合成增益尾端限制。缺少偏移基準時新增段只減益。這些政策仍不代表已確認 minimum-phase 或補償聲學抵消的可行性。
- 前級按整組密集網格最大合成增益加可設定餘裕估算，向下取到 0.1 dB。這不是所有訊號峰值／intersample clipping 保證，也不增加喇叭或擴大機的物理能力。DSP 取樣率是模擬條件，不是設備最大支援規格；本程式不控制播放器的 bit match 或重取樣。
- 手動修改使用相同政策重新驗算；逐 Band 移除比較不重算其他參數，增減益段可能互相補償。搜尋中曾接受的候選不一定是最終採用方案。沒有 `guard_policy="v5"` 的舊版仍按歷史政策解讀；事後新增診斷不代表當時已執行新限制，也不自動改寫舊參數。
- 主要優化一般 Peak／Bell 幅頻模型。沒有房間脈衝逆濾波、相位修正、FIR convolution、超低音分頻或時間對齊；沒有完整 house curve、喇叭自然低頻衰減或模態衰減時間目標。EQ 不能消除所有空間凹洞、反射或混響。
- CPU 深入搜尋使用 float64，搜尋與手動驗算共用穩定的 biquad 幅頻計算式；沒有 GPU 路徑或「算越久就越好聽」的保證。固定自動目標第 35 百分位、位置權重、增益支持平滑及保護門檻均未完成跨房間最佳化驗證。
- 同資料的求解及數值驗算只能確認模型內的一致性，不能代替獨立補錄、位置泛化或可比聆聽。使用者偏好可以作待解釋的觀察，尚未排除音量、曲目、適應等因素；沒有把任何既有 PEQ 當作客觀正解。
- 重錄差異使用各配對差值 RMS；更早保存的 `repeatability_db` 曾使用不同換算，兩筆重錄時比直接 A−B RMS 小 sqrt(2) 倍。歷史值保留，不宜直接跨版比較。
- 濾波器依設定步進取整後評估，仍需使用者確認實際 DSP 的 Hz／Gain／Q／頻寬定義。程式不會讀回硬體或播放器參數；匯出只有通用文字／CSV，沒有設備專用匯入、自動寫入 Roon 或其他 DSP、或自動檢測 PEQ 生效。「已套用」是使用者確認。
- 補錄比較條件、形狀與預測差異；未知或不一致條件可能只能標示需要確認。P−10／P+10 缺少同位置前測時，只能評估目前一致性，不能計算前後改善。新 PEQ 仍從 Baseline 求解，沒有自動學習補錄殘差或疊加上一版。

## 介面與資料保存

- 新計算在三組全部完成並驗證後，一次保存為一個可見版本；每組有獨立識別碼，切換查看不改變套用紀錄。整組刪除／還原。手動修改其中一組會另存獨立版本，不把舊比較排名套到手改結果。

- 目前以 Windows 桌面與深色 QML 介面為主要交付；其他作業系統、完整亮色主題、高對比與輔助技術尚未完成完整驗收。
- 圖表為互動顯示取樣，極窄的高頻細節應查看原始量測；專案仍保存完整解析數值。
- PEQ 刪除採軟刪除並可還原，不立即回收磁碟空間。歷史補錄、原方案與目前套用紀錄仍保留參照；版本名稱不重複利用。
- `.roompilot` 是包含資料與來源檔的本地專案包，沒有雲端同步、多人同時編輯或自動合併。匯入會建立副本。
- 專案包會驗證資料結構、版本與參照、有限數值、路徑及來源檔雜湊。這是完整性檢查，不是簽章、加密或實體量測真偽認證。
- 專案包上限為 1 GB 解壓資料量，`project.json` 上限為 150 MB。多次保存完整 Baseline 與歷史曲線會增加資料量。
- 預設資料存於 `%LOCALAPPDATA%\RoomPilot`。如要手動複製整個資料夾，先關閉程式以包含一致的 SQLite／WAL 狀態；跨電腦搬移優先使用專案包匯出。
